To create a bundle for OSX:

1.) Go into this folder with the terminal
2.) Execute the bash-script as follow „sh build_and_sign.sh“
3.) Pack the application into a dmg (DMG Canvas)



Information:
Change the value on line 10 of the bash-script, to yours apple-developer-certificate.

The bundle would be build with gradle.
